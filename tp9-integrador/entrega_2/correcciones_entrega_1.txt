 Bunas tardes!

Queríamos comentarles que la primer entrega del trabajo se encuentra aprobada y les pasamos algunas correcciones para tener en cuenta al realizar la presentación de la segunda parte.

    Debería contener una aclaración la interfaz gráfica que indique que solo se puede usar el directorio /archivos para poder tomar el archivo de prueba y generar el análisis. Pide un path y no se especifica nada, por lo tanto los primeros intentos de análisis por interfaz fueron fallidos.
    No detecta comentarios anidados. Ademas los comentarios no deben generar token, se ignoran.
    En la tabla de símbolos no se encuentran todas las entradas necesarias, faltan constantes reales y algunas constantes string.
    Cada lexema correspondiente a un tipo de dato debe generar un token único para cada uno, por ejemplo lexema float token TD_FLOAT, lexema int token TD_INT, etc.
    El control de longitudes en constantes numéricas y strings solo se ve al utilizar la consola, en la interfaz no genera ningún mensaje de salida.
    No genera ningún mensaje de error al encontrar un caracter no permitido en la interfaz gráfica, esto solo se ve al ejecutar por consola igual que el caso anterior.

